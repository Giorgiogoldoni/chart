name: RAPTOR Chart Data Update

on:
  schedule:
    - cron: '15 7 * * 1-5'    # 09:15 Italia (UTC+2 estate)
    - cron: '0 10 * * 1-5'    # 12:00 Italia
    - cron: '0 14 * * 1-5'    # 16:00 Italia
    - cron: '30 16 * * 1-5'   # 18:30 Italia
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install "yfinance>=0.2.40" pandas

      - name: Run fetch
        run: python fetch.py

      - name: Commit and push data
        run: |
          git config user.name  "RAPTOR Bot"
          git config user.email "raptor-bot@users.noreply.github.com"
          git add data/
          git diff --staged --quiet || git commit -m "📊 RAPTOR data $(date -u '+%d/%m/%Y %H:%M')"
          git push
